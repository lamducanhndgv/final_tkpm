// let host = 'http://42601a6b7824.ngrok.io';
let host = 'http://localhost:8888';

let logURL = host + '/login';
let regURL = host + '/register';
let subscribeURL = host + '/subscribe';
let updateURL = host + '/update';
let hostURL = host;
let logoutURL = host + '/logout';